#include <iostream>
#include <locale>
#include "ProgressBar.h"
#include "Console.h"

using namespace std;

int testar() {
	/*setlocale(LC_ALL, "pt-BR");
	ProgressBar vendasProgreeBar(6, 5, 40), investidoresProgreeBar(6, 10, 80);

	Console::clrscr();
	Console::textColor(Background::BLACK, Foreground::WHITE);
	Console::gotoxy(1, 1); cout << "Importando os dados do Tesouro Direto...";

	Console::gotoxy(3, 4); cout << "1. Vendas do Tesouro Direto";
	for (int p = 1; p <= 200; p++) 
		vendasProgreeBar.show(p / 200.0 * 100);
	
	Console::gotoxy(3, 9); cout << "2. Investidores do Tesouro Direto";
	for (int p = 1; p <= 2000; p++)
		investidoresProgreeBar.show(p / 2000.0 * 100);

	cout << endl << endl;
	Console::textColor(Background::BLACK, Foreground::LIGHT_GRAY);
	cout << endl;*/
	return 0;
}

/*int main() {
	return testar();
}*/